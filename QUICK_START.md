# TrackPoint 功耗优化 - 快速使用指南

## 简介
此更新为 UART 版 PS/2 TrackPoint 驱动添加了智能功耗管理功能，可以在不使用时自动降低功耗。

## 核心改进

### 1. 智能空闲检测
- 自动检测用户活动
- 默认30秒无操作后进入节能模式
- 智能过滤背景噪声

### 2. 动态电源管理
- 空闲时停止数据传输
- 检测到活动时快速恢复
- 预期功耗降低80-90%

## 使用方法

在您的配置文件（通常是 `.conf` 文件）中添加：
```
CONFIG_ZMK_INPUT_MOUSE_PS2_IDLE_THRESHOLD_MS=30000
```

### 可选配置值（毫秒）：
- `15000` (15秒) - 快速响应，适合移动办公
- `30000` (30秒) - 平衡设置，推荐默认值
- `60000` (60秒) - 最小延迟，适合游戏使用

### 可选配置值（毫秒）：
- `15000` (15秒) - 快速响应，适合移动办公
- `30000` (30秒) - 平衡设置，推荐默认值
- `60000` (60秒) - 最小延迟，适合游戏使用

## 预期效果

### 功耗改善
- **空闲状态**: 从 ~5mA 降至 ~0.5-1mA
- **正常使用**: 从 ~5mA 降至 ~1-2mA
- **电池续航**: 提升1.5-3倍

## 验证功能

编译固件后，您应该能在日志中看到：
```
[INF] Entering idle mode to reduce power consumption
[INF] Exiting idle mode - user activity detected
```

## 故障排除

### 如果响应有延迟：
增加配置值：`CONFIG_ZMK_INPUT_MOUSE_PS2_IDLE_THRESHOLD_MS=60000`

### 如果频繁进入/退出空闲模式：
- 检查TrackPoint是否有物理振动
- 适当增加阈值时间
- 确保没有背景干扰

### 如果功耗改善不明显：
- 确认配置已正确应用
- 检查其他高功耗组件
- 启用调试日志查看详细信息

## 技术说明

### 核心功能
- 智能活动检测（过滤微小噪声移动）
- 自动节能模式切换
- 快速恢复机制（<100ms）

### 兼容性
- 与现有按键映射完全兼容
- 不影响层切换功能
- 支持所有PS/2 TrackPoint设备

这个优化应该能显著改善您的设备电池续航！